/*These are all the actions available in the application
They are fired to change the state*/

export const logout = () => ({
  type: 'LOGOUT'
})

export const login = () => ({
  type: 'LOGIN'
})
